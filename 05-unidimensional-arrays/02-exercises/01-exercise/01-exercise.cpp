/**
 * Write a program that defines an array of numbers and calculate the sum of it's elements
*/

#include <iostream>

using namespace std;

int main () {

    int numbers[] = {4, 5, 6, 9};

    cout << "Result: " << numbers[0] + numbers[1] + numbers[2] + numbers[3] << endl;

    return 0;
}