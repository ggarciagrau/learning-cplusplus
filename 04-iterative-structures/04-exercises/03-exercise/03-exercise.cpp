/**
 * Display in the stdout the sum of the first ten integers greater than 0 
*/

#include <iostream>
#include <tgmath.h>

using namespace std;

int main () {

    int result = 0;

    for (int i = i; i <= 10; i++)
    {
        result += pow(i, 2);
    }

    cout << "Result: " << result << endl;

    return 0;
}